# Merge this handle_path block into the existing HTTPS site block.
# Do not replace unrelated routes for the domain.
example.com {
    handle_path /dawnpilot/* {
        encode zstd gzip
        reverse_proxy 127.0.0.1:8787
    }
}
